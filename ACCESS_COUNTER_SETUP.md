# 累計アクセス数の設定

サイト内に「秘密基地の来場者数」欄を追加しています。

Cloudflare PagesでKVを1つ作成して、Pages Functionsに紐づけます。

Binding name:

```txt
SITE_STATS
```

この名前が違うとアクセス数は「準備中」のままになります。

仕様:
- サイトを開くたびに1カウント増えます。
- 自分のアクセス、再読み込み、botも含みます。
- 正確なユニークユーザー数ではなく、ページ表示回数に近い数字です。
